let minval = Math.min(2, 9)
game.splash("The minimum value is " + minval)
let remainder = 4 % 2
if (remainder == 0) {
    game.splash("The integer is " + "evenly divisible")
} else {
    game.splash("The remainder is " + remainder)
}
